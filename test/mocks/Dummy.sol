pragma solidity ^0.5.0;

import "@ensdomains/ens/contracts/ENSRegistry.sol";
import "@ensdomains/ens/contracts/Deed.sol";
import "@ensdomains/ethregistrar/contracts/BaseRegistrarImplementation.sol";

// It's here, because of imports that are used in tests, but not in contracts. Otherwise truffle won't find them

contract Dummy {

}
